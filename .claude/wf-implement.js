export const meta = {
  name: 'chatpage-implement',
  description: 'Implement the audited improvement blueprint: one agent per file (no edit conflicts), then verify (typecheck+build+test) and repair until green',
  phases: [
    { title: 'Implement', detail: 'one agent per file, parallel' },
    { title: 'Verify', detail: 'typecheck + build + test + lint, repair loop' },
  ],
}

const CONTEXT = "PROJECT: /web/xenforo-ai-chatpage — a TypeScript React 19 chat UI embedded in the XenForo forum at windowsforum.com/chatpage. Stack: Vite 8 + React 19 + MUI 9 + Emotion + marked + DOMPurify. cwd is the project root. The working tree already has GOOD in-flight uncommitted improvements (better SSE event-boundary parsing, conversation-history serialization, localStorage pruning, CaptchaRequiredError, refusal events) — PRESERVE them; never revert them. 'npx vite build' currently succeeds and MUST stay green. Env vars are Vite-style (import.meta.env.VITE_*)."
const CONTRACT = "These shared interfaces are PINNED so cross-file usage aligns exactly. Use these exact names/shapes.\n\n// ADD to src/types/index.ts (as exports):\nexport interface ErrorResponse {\n  error?: string;\n  captcha_required?: boolean;\n  [key: string]: unknown;\n}\nexport interface SSEAnnotation {\n  type?: string;\n  filename?: string;\n  file_id?: string;\n}\nexport interface SSEEvent {\n  type?: string;\n  delta?: string;\n  text?: string;\n  refusal?: string;\n  annotation_index?: number;\n  annotation?: SSEAnnotation;\n  part?: { annotations?: SSEAnnotation[] };\n  response_id?: string;\n  id?: string;\n  response?: { id?: string; error?: { message?: string }; incomplete_details?: { reason?: string } };\n  detail?: string;\n  error?: string | { message?: string };\n  message?: string;\n  [key: string]: unknown;\n}\n// MessageProps ALSO gets:  isLastUserMessage?: boolean;\n\n// APPEND to src/vite-env.d.ts (keep the existing /// <reference types=\"vite/client\" /> line first).\n// Provides Web Speech + Turnstile globals WITHOUT @types/web:\ndeclare global {\n  interface SpeechRecognitionAlternative { readonly transcript: string }\n  interface SpeechRecognitionResult { readonly length: number; readonly [index: number]: SpeechRecognitionAlternative }\n  interface SpeechRecognitionResultList { readonly length: number; readonly [index: number]: SpeechRecognitionResult }\n  interface SpeechRecognitionEvent extends Event { readonly results: SpeechRecognitionResultList }\n  interface SpeechRecognitionErrorEvent extends Event { readonly error: string }\n  interface SpeechRecognition extends EventTarget {\n    interimResults: boolean;\n    lang: string;\n    onresult: ((event: SpeechRecognitionEvent) => void) | null;\n    onerror: ((event: SpeechRecognitionErrorEvent) => void) | null;\n    onend: (() => void) | null;\n    start(): void;\n    stop(): void;\n  }\n  interface SpeechRecognitionConstructor { new (): SpeechRecognition }\n  interface Window {\n    SpeechRecognition?: SpeechRecognitionConstructor;\n    webkitSpeechRecognition?: SpeechRecognitionConstructor;\n    turnstile?: {\n      render: (selector: string, config: Record<string, unknown>) => string;\n      reset: (widgetId: string) => void;\n    };\n  }\n}\nexport {};"
const RULES = "1. You OWN exactly ONE file (named below). Edit/create ONLY that file. The smoke-test agent additionally owns a new root vitest.config.ts (stated in its notes) — every other agent: single file only. Touching another agent's file causes conflicts.\n2. READ the current file before editing. Apply changes by CONTENT/semantics; the line numbers in instructions are hints from an earlier snapshot and may be slightly off after the in-flight edits.\n3. Make ONLY the listed changes. Preserve all other behavior. Match the surrounding code style (quotes, spacing, no semicolon/style churn).\n4. Keep TypeScript valid: both 'npx tsc --noEmit' and 'npx vite build' must stay green. If a change would break tsc, adjust the typing to achieve the same intent while compiling (e.g. narrow a union) rather than dropping the change.\n5. Use the EXACT shared interface names/shapes from the TYPE CONTRACT. Do NOT add @types/web — Speech/Turnstile types are global via vite-env.d.ts.\n6. Do NOT run npm install. Do NOT commit, push, or deploy. Do NOT edit package.json (already finalized) or tsconfig unless tsconfig IS your assigned file.\n7. End by stating concisely what you changed (this is your return value, not shown to the user)."
const PACKETS = [{"file": "src/components/ChatWindow.tsx", "isNew": false, "rationale": "The 709-line main component is the locus of most correctness, a11y, perf, and UX findings. Colocated so a single agent applies them without conflicting with api.ts/helpers.ts/types work. Several findings are duplicates and are merged.", "changes": [{"title": "Wrap localStorage save effect in try/catch (quota + private-mode safety)", "severity": "high", "instruction": "In the save effect (lines 161-164) wrap both localStorage.setItem calls in one try/catch. On error, if (error instanceof Error && error.name === 'QuotaExceededError') setErrorMessage('Storage quota exceeded. Your conversation may not persist across page reloads.'); else console.error('Failed to save conversations to localStorage:', error). Keep keys/values and deps [conversations, currentConversationId]."}, {"title": "Validate + sanitize loadSavedConversations (shape + prototype-pollution guard) — MERGED with localStorage-json-parse-no-validation", "severity": "high", "instruction": "Rewrite loadSavedConversations (lines 83-93) to validate structure AND filter dangerous keys (merges the two localStorage-validation findings). Body: try { const saved = localStorage.getItem('chat_conversations'); if (!saved) return {}; const parsed = JSON.parse(saved); if (!parsed || typeof parsed !== 'object') return {}; const sanitized: ConversationMap = {}; for (const [id, conv] of Object.entries(parsed as Record<string, unknown>)) { if (typeof id !== 'string' || id === '__proto__' || id === 'constructor' || id === 'prototype') continue; const c = conv as Partial<Conversation>; if (c && typeof c === 'object' && typeof c.id === 'string' && typeof c.title === 'string' && Array.isArray(c.messages) && typeof c.createdAt === 'number' && typeof c.updatedAt === 'number') { sanitized[id] = c as Conversation; } } return sanitized; } catch (error) { console.warn('Ignoring invalid saved conversations:', error); return {}; }. Conversation/ConversationMap already imported. Preserves the empty-object fallback."}, {"title": "Make Turnstile script-load effect idempotent and StrictMode-safe", "severity": "high", "instruction": "Replace the Turnstile script-loading effect (lines 509-517): const existing = document.querySelector('script[src*=\"turnstile\"]'); if (existing) return; const script = document.createElement('script'); script.src = 'https://challenges.cloudflare.com/turnstile/v0/api.js'; script.async = true; document.body.appendChild(script); return () => { if (document.body.contains(script)) document.body.removeChild(script); }. Keep deps []. Prevents StrictMode double-invoke NotFoundError and a duplicate script."}, {"title": "Fix stale-closure resend after captcha using inputRef", "severity": "high", "instruction": "Add `const inputRef = useRef(input);` near line 155 plus `useEffect(() => { inputRef.current = input; }, [input]);`. In the Turnstile callback (lines 537-539) change `if (input) { handleSendMessage(); }` to `if (inputRef.current) { handleSendMessage(inputRef.current); }` (handleSendMessage accepts an optional messageContent first arg, lines 283-291). Leave the rest of the effect intact."}, {"title": "Add ARIA live region to the messages container", "severity": "high", "instruction": "On the messages container Box (lines 605-611, className=chat-messages-container) add role=\"log\", aria-live=\"polite\", aria-label=\"Chat messages\". Do not alter sx or scroll behavior."}, {"title": "Honor prefers-reduced-motion for the example-prompts Fade", "severity": "high", "instruction": "Add a local hook: const [reduceMotion, setReduceMotion] = useState(() => window.matchMedia('(prefers-reduced-motion: reduce)').matches); useEffect(() => { const mq = window.matchMedia('(prefers-reduced-motion: reduce)'); const h = (e: MediaQueryListEvent) => setReduceMotion(e.matches); mq.addEventListener('change', h); return () => mq.removeEventListener('change', h); }, []);. On the <Fade in={true}> at line 661 pass timeout={reduceMotion ? 0 : undefined}. CSS keyframe/transition disabling is in the App.css packet."}, {"title": "Surface TTS playback errors to the user", "severity": "high", "instruction": "In playAudioResponse catch (lines 259-261) keep console.error and add setErrorMessage('Failed to play audio. Please try again later.'). Do not add setErrorMessage to the useCallback deps (setters are stable)."}, {"title": "Theme-aware border for the file-annotation Sources block", "severity": "high", "instruction": "At line 368 replace the hardcoded `border-top: 1px solid rgba(255,255,255,0.2)` in the citationDetails wrapper with `border-top: 1px solid ${borderColor};` using the existing borderColor variable (line 158). Keep this file-annotation path separate from the helpers.ts markdown-citation path."}, {"title": "Make example-prompt Chips keyboard-accessible", "severity": "medium", "instruction": "On the Chip at line 669 add the prop clickable (MUI sets role=button, tabIndex=0, Enter/Space handlers). onClick and sx hover unchanged."}, {"title": "Persist mute state to localStorage", "severity": "medium", "instruction": "Change isMuted initializer (line 145) to `useState<boolean>(() => { try { return JSON.parse(localStorage.getItem('chat_mute') ?? 'true'); } catch { return true; } })`. Refactor toggleMute (line 506) to `useCallback(() => { setIsMuted(prev => { const next = !prev; try { localStorage.setItem('chat_mute', JSON.stringify(next)); } catch {} return next; }); }, [])`."}, {"title": "Allow editing the most recent user message while AI streams", "severity": "medium", "instruction": "Compute `const lastUserMessageId = useMemo(() => { for (let i = currentConversation.messages.length - 1; i >= 0; i--) { if (currentConversation.messages[i].role === 'user') return currentConversation.messages[i].id; } return undefined; }, [currentConversation.messages]);`. Pass `isLastUserMessage={msg.id === lastUserMessageId}` to the non-streaming MessageComponent (lines 614-623). The Message.tsx and types packets add/consume the optional prop. Do not pass it to the streaming MessageComponent."}, {"title": "Guard the abort-controller ref clear against overlapping requests", "severity": "medium", "instruction": "The local abortController is captured (lines 301-302). In the finally block (line 418) change `abortControllerRef.current = null;` to `if (abortControllerRef.current === abortController) { abortControllerRef.current = null; }`."}, {"title": "Stabilize streaming render + callbacks — MERGED with streaming-message-rerenders-due-to-object-spread and message-memo-prop-instability", "severity": "medium", "instruction": "Keep streaming rendering via dangerouslySetInnerHTML (do NOT switch to plain <pre>; out of scope). (1) Memoize the streaming display object: replace the inline object passed to the streaming MessageComponent (lines 626-639) with `const streamingDisplayMsg = useMemo(() => streamingMessage ? { ...streamingMessage, content: sanitizeAndParse(streamingMessage.content || '▍') } : null, [streamingMessage]);` and render `{streamingDisplayMsg && (<MessageComponent msg={streamingDisplayMsg} ... />)}`. (2) Stop memoized Message rows re-rendering each delta: add `const streamingMessageRef = useRef(streamingMessage);` with `useEffect(() => { streamingMessageRef.current = streamingMessage; }, [streamingMessage]);`, read streamingMessageRef.current instead of the closure variable in handleStopGeneration (lines 270-275) and at lines 411-412, and REMOVE streamingMessage from handleSendMessage's deps (line 430) and handleStopGeneration's deps. Verify abort still appends ' [Generation stopped]'."}, {"title": "Stabilize the currentConversation fallback object", "severity": "low", "instruction": "Extract the inline fallback (lines 129-135) into `const defaultConversation = useMemo<Conversation>(() => ({ id: currentConversationId, title: 'New Chat', messages: [], createdAt: Date.now(), updatedAt: Date.now() }), [currentConversationId]);` before line 128, then `const currentConversation = useMemo(() => conversations[currentConversationId] || defaultConversation, [conversations, currentConversationId, defaultConversation]);`."}, {"title": "Remove dead result===null branch — MERGED with result-null-check-impossible", "severity": "low", "instruction": "ChatAPI.sendMessage never resolves to null, so the `else if (result === null)` branch (lines 379-380) is dead. Collapse lines 356-385: keep `if (result && result.text) { ... }` unchanged, then a single `else { setErrorMessage('No response received from server. Please try again.'); setStreamingMessage(null); console.error('Empty result from sendChatMessage:', result); }`."}, {"title": "Type SpeechRecognition state and handlers", "severity": "high", "instruction": "After @types/web (package.json packet) and Window globals (vite-env.d.ts packet): line 463 -> `const [speechRecognition, setSpeechRecognition] = useState<SpeechRecognition | null>(null);`. In the effect: `const SpeechRecognitionCtor = window.SpeechRecognition || window.webkitSpeechRecognition;` guard `if (SpeechRecognitionCtor)`, `const recognition = new SpeechRecognitionCtor();`. Type onresult `(event: SpeechRecognitionEvent) => {` and map `Array.from(event.results).map((result) => result[0].transcript)` (drop `(result: any)`). Type onerror `(event: SpeechRecognitionErrorEvent) => {`. If @types/web conflicts with @types/node and breaks tsc, fall back to minimal interfaces in vite-env.d.ts. No runtime change."}, {"title": "Remove window-as-any casts for Turnstile/SpeechRecognition", "severity": "medium", "instruction": "After the Window augmentation lands (vite-env.d.ts packet) remove the four (window as any) casts at lines 466, 521, 523, 527. Use window.turnstile directly: `if (showCaptcha && window.turnstile)`, `if (turnstileWidgetRef.current) { window.turnstile.reset(turnstileWidgetRef.current); return; }`, `turnstileWidgetRef.current = window.turnstile.render('#turnstile-container', { ... })`."}], "extraNotes": "TYPES NOTE: SpeechRecognition, SpeechRecognitionEvent, SpeechRecognitionErrorEvent and window.SpeechRecognition / window.webkitSpeechRecognition / window.turnstile are declared GLOBALLY in src/vite-env.d.ts. Do NOT import them and do NOT use @types/web. Use them directly and remove every `(window as any)` cast and the `useState<any>` for speechRecognition. This is the LARGEST packet (17 changes) and they interact within one file — apply them as ONE coherent set, then re-read the whole component to confirm: streaming still renders, Stop appends ' [Generation stopped]', edit/regenerate rebuild history, captcha resend uses the LIVE input via inputRef. isLastUserMessage is passed to the non-streaming Message rows only."}, {"file": "src/services/api.ts", "isNew": false, "rationale": "All API-layer type-safety and SSE schema-validation findings live here, independent of component edits. Introduces typed payload/error/event shapes and a BackendError class while preserving the in-flight SSE event-boundary parser exactly.", "changes": [{"title": "Replace parsedData: any with a discriminated SSE event union and add schema guards", "severity": "high", "instruction": "Define SSEEvent union (in src/types/index.ts per its packet, imported here, OR locally) with members: response.output_text.delta {delta?:string}; response.output_text.done {text?:string}; response.refusal.delta {delta?:string}; response.refusal.done {refusal?:string}; response.output_text.annotation.added {annotation?:{type?:string;filename?:string;file_id?:string};annotation_index?:number}; response.content_part.done {part?:{annotations?:Array<{filename?:string;file_id?:string}>}}; response.completed; error|function_call_error {detail?:string;error?:string;message?:string}; response.failed|response.incomplete {response?:{error?:{message?:string};incomplete_details?:{reason?:string}}}; and catch-all {type:string;[key:string]:unknown}. Change line 150-152 to `let parsedData: SSEEvent;` / `parsedData = JSON.parse(jsonStr) as SSEEvent;`. Harden array access: at line 220 guard `if (Array.isArray(parsedData.part?.annotations))` before .map(); at line 210 only push when `parsedData.annotation?.type === 'file_citation' && typeof parsedData.annotation.filename === 'string'`. Preserve responseId extraction (line 162) and all event handling."}, {"title": "Type error responses with ErrorResponse instead of any", "severity": "high", "instruction": "Import ErrorResponse (types packet: { error?: string; captcha_required?: boolean; [key: string]: unknown }). Change line 28 and line 103 `let errorData: any;` to `let errorData: ErrorResponse;`. Property access unchanged."}, {"title": "Type the sendMessage payload", "severity": "high", "instruction": "Add interface SendMessagePayload { message: string; captcha_token?: string; client_conversation_id?: string; reset_conversation?: boolean; history?: ChatMessageHistoryItem[]; }. Change line 80 `const payload: any = { message };` to `const payload: SendMessagePayload = { message };`. Conditional additions (81-92) unchanged."}, {"title": "Introduce BackendError class to replace any-cast error objects", "severity": "medium", "instruction": "Add `export class BackendError extends Error { readonly isBackendError = true; constructor(message: string) { super(message); this.name = 'BackendError'; } }` near CaptchaRequiredError. Replace the patterns at lines 169-171 and 179-181 with `throw new BackendError(...)` using the identical message strings (`AI service error: ${errorDetail}`, `AI service error: ${detail}`) so ChatWindow string matching still works."}, {"title": "SSE schema validation (covered by union + array guards)", "severity": "medium", "instruction": "Satisfied by the discriminated-union change: ensure the Array.isArray guard (line 220) and the file_citation/filename guards (line 210). No separate validator needed; do not change default/fall-through behavior for unknown event types."}], "extraNotes": "NARROWING NOTE: SSEEvent.error is typed `string | { message?: string }`. In the response.failed/incomplete branch that reads `.message`, add a tiny helper `const msgOf = (e: SSEEvent['error']) => (typeof e === 'string' ? e : e?.message);` and use `msgOf(parsedData.error)`. In the error/function_call_error branch use `parsedData.detail || (typeof parsedData.error === 'string' ? parsedData.error : parsedData.error?.message) || parsedData.message`. Import the shared types (SSEEvent, SSEAnnotation, ErrorResponse) from '../types' exactly as named in the TYPE CONTRACT. Keep BackendError exported from THIS file."}, {"file": "src/utils/helpers.ts", "isNew": false, "rationale": "Security (rel attrs, markdown-escaping), UX (theme-neutral markdown citation border), a11y (streaming caret), and perf (early-return) all touch sanitizeAndParse. The duplicate-citation finding is resolved by fixing the border here while keeping the two data paths separate.", "changes": [{"title": "Add noreferrer to target=_blank links", "severity": "medium", "instruction": "Change rel=\"noopener\" to rel=\"noopener noreferrer\" on line 27 (inline anchor) and line 43 (citation-list anchor). DOMPurify allows both."}, {"title": "Make the markdown Sources border visible in both themes", "severity": "medium", "instruction": "helpers.ts has no React theme. At line 45 replace `border-top: 1px solid rgba(255,255,255,0.2)` with `border-top: 1px solid rgba(128,128,128,0.35)` (neutral grey visible on light and dark). Keep separate from the ChatWindow file-annotation path; do not merge data logic."}, {"title": "Escape markdown-special chars in citation text", "severity": "low", "instruction": "Add `const escapeMarkdown = (s: string) => s.replace(/[\\\\\\[\\]()]/g, '\\\\$&');` and apply it to c.text in the citation-list anchor (line 43). Do not alter URLs."}, {"title": "Add early-return + short-circuit fast paths", "severity": "low", "instruction": "At the top of sanitizeAndParse (lines 13-14) add `if (!content) return content;`. Only run the citationPattern.replace block when `content.includes('[')` and the bareDomainPattern.replace block when `processedContent.includes('(')`. Output identical when those chars present; marked()+DOMPurify still always run."}, {"title": "Mark the streaming caret decorative for screen readers", "severity": "low", "instruction": "Change the final lines to `const rendered = marked(processedContent) as string; let sanitized = DOMPurify.sanitize(rendered, { ADD_ATTR: ['aria-hidden'] });` then `if (sanitized.trim() === '<p>▍</p>') { sanitized = '<p><span aria-hidden=\"true\">▍</span></p>'; } return sanitized;`. Use U+258D (▍), the caret ChatWindow passes."}], "extraNotes": ""}, {"file": "src/components/Message.tsx", "isNew": false, "rationale": "All five icon-button a11y labels, the link-color contrast fix, copy-feedback timing, and the edit-button visibility prop are isolated to this component.", "changes": [{"title": "Add aria-label to all icon-only action buttons", "severity": "high", "instruction": "Add aria-label: Copy (line 163) 'Copy message content'; Edit (line 170) 'Edit message'; Regenerate (line 178) 'Regenerate response'; Thumbs up (line 187) 'Good response'; Thumbs down (line 196) 'Bad response'. Keep the Tooltips."}, {"title": "Remove hardcoded low-contrast link color override", "severity": "medium", "instruction": "Delete the `'& a': { color: '#4299E1', textDecoration: 'none', '&:hover': { textDecoration: 'underline' } }` block from the message-content sx (lines 141-147) so accessible App.css link colors (#0969da / #58a6ff) cascade. Keep '& pre' and '& code'."}, {"title": "Consume isLastUserMessage prop for the edit button", "severity": "medium", "instruction": "Add isLastUserMessage to destructured props (optional in MessageProps per the types packet). Change the edit-button condition at line 168 from `{msg.role === 'user' && isLastMessage && (` to `{msg.role === 'user' && isLastUserMessage && (`. Keep regenerate (line 176) on isLastMessage. When isLastUserMessage is undefined the edit button simply does not render."}, {"title": "Extend copy-confirmation visibility", "severity": "low", "instruction": "Change the timeout on line 47 from 2000 to 3500."}], "extraNotes": ""}, {"file": "src/components/ConversationSidebar.tsx", "isNew": false, "rationale": "Single a11y fix for the delete button; isolated.", "changes": [{"title": "Add aria-label to sidebar delete button", "severity": "high", "instruction": "Add aria-label=\"Delete conversation\" to the IconButton at line 84. Leave the e.stopPropagation() onClick and .delete-btn opacity behavior unchanged."}], "extraNotes": ""}, {"file": "src/types/index.ts", "isNew": false, "rationale": "Central home for the shared interfaces consumed by api.ts (ErrorResponse, SSEEvent) and Message.tsx (isLastUserMessage), matching the project's props/types convention.", "changes": [{"title": "Add ErrorResponse interface", "severity": "high", "instruction": "Add `export interface ErrorResponse { error?: string; captcha_required?: boolean; [key: string]: unknown; }`."}, {"title": "Add SSEEvent discriminated union (preferred central location)", "severity": "high", "instruction": "Declare and export the SSEEvent union exactly as specified in the api.ts packet so api.ts can import it. Define it in exactly one place — if kept local to api.ts instead, omit here to avoid an unused export."}, {"title": "Add optional isLastUserMessage to MessageProps", "severity": "medium", "instruction": "In MessageProps (lines 47-56) add `isLastUserMessage?: boolean;` (optional)."}], "extraNotes": "Add the EXACT interfaces from the TYPE CONTRACT (ErrorResponse, SSEAnnotation, SSEEvent) as `export`s, plus `isLastUserMessage?: boolean;` in MessageProps. Do not define SSEEvent anywhere else; api.ts imports it from here."}, {"file": "src/config/env.ts", "isNew": false, "rationale": "Env-validation fail-fast in PROD is isolated here. The orphaned VITE_DATA_DOMAIN cleanup belongs in the .env.example packet since env.ts never referenced it.", "changes": [{"title": "Fail fast in production on missing required env vars", "severity": "medium", "instruction": "Replace the console.warn block (lines 41-45) with: `if (missingVars.length > 0) { if (import.meta.env.PROD) { throw new Error(\\`Critical configuration error: Missing required environment variables: ${missingVars.join(', ')}.\\`); } else { console.warn(\\`Missing environment variables: ${missingVars.join(', ')}. Using defaults.\\`); } }`. Keep requiredVars = ['VITE_DOMAIN'] (line 36). Current .env defines VITE_DOMAIN so normal builds are unaffected."}], "extraNotes": ""}, {"file": "tsconfig.json", "isNew": false, "rationale": "Single packet merging all four baseUrl-deprecation findings (build/types/docs flag the same TS5101). Removing baseUrl is cleanest: no source uses path-based imports and Vite has its own alias resolver.", "changes": [{"title": "Remove deprecated baseUrl (fixes TS5101 / tsc --noEmit)", "severity": "high", "instruction": "Delete line 26 `\"baseUrl\": \"src\"` and remove the trailing comma now needed on line 25 (`\"isolatedModules\": true` becomes the last option). Do NOT add paths or ignoreDeprecations — imports are all relative and vite.config.ts resolve.alias is independent. Verify `npx tsc --noEmit` passes alongside the api.ts/types/ChatWindow type fixes."}], "extraNotes": ""}, {"file": "deploy.sh", "isNew": false, "rationale": "Single build-safety fix; isolated.", "changes": [{"title": "Abort deploy if the build fails (before the destructive rm)", "severity": "high", "instruction": "Change line 9 `npx vite build` to `npx vite build || { echo \"Build failed! Aborting deploy.\"; exit 1; }` so the script never reaches `rm -rf /web/public_html/chatpage/*` (line 13) on a failed build."}], "extraNotes": ""}, {"file": "vite.config.ts", "isNew": false, "rationale": "Build-tooling improvements (sourcemaps, vendor-chunk split) isolated to the Vite config. The consistent-filename output is preserved.", "changes": [{"title": "Split vendor chunk into react / mui / emotion", "severity": "low", "instruction": "In manualChunks (lines 27-37) return distinct names: `if (id.includes('node_modules/@mui/')) return 'mui'; if (id.includes('node_modules/@emotion/')) return 'emotion'; if (id.includes('node_modules/react/') || id.includes('node_modules/react-dom/')) return 'vendor';`. Vite regenerates modulepreload links; chunkFileNames static/js/[name].chunk.js keeps deterministic names. Confirm `vite build` emits mui.chunk.js / emotion.chunk.js."}], "extraNotes": "ONLY apply the vendor-chunk split (change [2]): manualChunks returns 'mui' for node_modules/@mui/, 'emotion' for node_modules/@emotion/, 'vendor' for node_modules/react/ or react-dom/. DO NOT enable sourcemaps — keep `sourcemap: false` (deliberate product decision; .map files must NOT be emitted to the public dist/). Leave entryFileNames/chunkFileNames/assetFileNames untouched."}, {"file": "src/App.css", "isNew": false, "rationale": "The CSS half of the prefers-reduced-motion finding lives here; the MUI Fade half is in the ChatWindow packet. Isolated to one stylesheet.", "changes": [{"title": "Disable animations/transitions/smooth-scroll under prefers-reduced-motion", "severity": "high", "instruction": "Append one `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0.001ms !important; animation-iteration-count: 1 !important; transition-duration: 0.001ms !important; scroll-behavior: auto !important; } }` block at the end. Covers pulse/fadeInUp/messageSlideIn/loadingDots (lines 220-235, 268-297, 342-357), button/icon/chip/drawer transitions (239, 243, 253, 264), and .chat-messages-container smooth scroll (216) without removing motion for users who have not opted out."}], "extraNotes": ""}, {"file": "src/vite-env.d.ts", "isNew": false, "rationale": "Existing one-line file augmented with a global Window declaration so the window-as-any casts can be removed from ChatWindow. A targeted addition, not a new file.", "changes": [{"title": "Declare runtime-injected Window globals (turnstile + Speech APIs)", "severity": "medium", "instruction": "Below the existing `/// <reference types=\"vite/client\" />` add: `declare global { interface Window { SpeechRecognition?: typeof SpeechRecognition; webkitSpeechRecognition?: typeof SpeechRecognition; turnstile?: { render: (selector: string, config: Record<string, unknown>) => string; reset: (widgetId: string) => void; }; } } export {};`. If @types/web is NOT installed (package.json fallback), declare a minimal SpeechRecognition shape and type the Window props against it. Ensure `tsc --noEmit` stays green."}], "extraNotes": "EXACT REQUIRED CONTENT — the file currently contains only `/// <reference types=\"vite/client\" />`. Keep that line and APPEND the global declarations from the TYPE CONTRACT verbatim (SpeechRecognition* interfaces + Window augmentation for SpeechRecognition/webkitSpeechRecognition/turnstile), ending with `export {};`. Do NOT add @types/web. `tsc --noEmit` MUST stay green."}, {"file": ".env.example", "isNew": false, "rationale": "Orphaned VITE_DATA_DOMAIN documented but never read by env.ts. Cleanup belongs here, not in env.ts.", "changes": [{"title": "Remove orphaned VITE_DATA_DOMAIN from the template", "severity": "medium", "instruction": "Delete the `VITE_DATA_DOMAIN=https://data.windowsforum.com` line from .env.example. env.ts never references it. Leave VITE_DOMAIN and VITE_TEST_DOMAIN."}], "extraNotes": ""}, {"file": "CLAUDE.md", "isNew": false, "rationale": "Doc corrections from stale Create-React-App content to the real Vite/VITE_* setup. CLAUDE.md and AGENTS.md are byte-identical in the affected sections (both 273 lines, same line numbers); apply identical edits.", "changes": [{"title": "Correct Development Commands, Project Configuration, Testing, and Build Artifacts sections", "severity": "high", "instruction": "Development Commands (lines 8-12): replace with `npm run dev` (Vite, http://localhost:5173), `npm run build` (vite build -> dist/, consistent filenames, no hashes), `npm run preview`, `npm run typecheck` (tsc --noEmit), `npm run deploy`. Remove npm start, npm test, npm run build:consistent. Deployment (line 15): remove the build.sh hash-suffix sentence; state consistent filenames are produced by vite.config.ts rollupOptions and there is no build.sh. Project Configuration (18-21): Framework -> 'Vite 8 + React 19 + TypeScript'; Build Output -> 'dist/ directory (consistent filenames, no content hashes)'; Base Path -> '/chatpage (configured in vite.config.ts base field)'; Environment -> 'Variables in .env must be prefixed with VITE_'. Testing Deployment Locally (248-251): step 1 -> npm run build, step 2 -> Check dist/static/js/main.js exists (no hash), step 3 -> npm run preview. Build Artifacts (269-272): replace the build.sh paragraph with text stating vite.config.ts rollupOptions output static/js/main.js, static/css/main.css, static/media/[name][extname], no hashes, intentional for XenForo integration."}, {"title": "Correct env-var prefixes to VITE_*", "severity": "high", "instruction": "Environment Variables block (lines 154-159): rename REACT_APP_DOMAIN->VITE_DOMAIN, REACT_APP_TEST_DOMAIN->VITE_TEST_DOMAIN, REACT_APP_TURNSTILE_SITE_KEY->VITE_TURNSTILE_SITE_KEY, REACT_APP_ENABLE_VOICE->VITE_ENABLE_VOICE, REACT_APP_MAX_CONVERSATIONS->VITE_MAX_CONVERSATIONS, and DELETE the REACT_APP_DATA_DOMAIN line (line 156). Common Pitfalls (line 243): change 'Must start with REACT_APP_' to 'Must start with VITE_ to be accessible (e.g., VITE_DOMAIN)'."}, {"title": "Fix framework identifier", "severity": "high", "instruction": "Line 18: change '**Framework**: Create React App with TypeScript' to '**Framework**: Vite 8 + React 19 + TypeScript'."}, {"title": "Fix the JSX-transform pitfall note", "severity": "medium", "instruction": "Line 240: replace pitfall #3 with 'React imports are optional for JSX - Vite @vitejs/plugin-react with jsx: react-jsx in tsconfig.json enables the automatic JSX transform; still import React (or its hooks) when you use them.'"}], "extraNotes": ""}, {"file": "AGENTS.md", "isNew": false, "rationale": "AGENTS.md is byte-identical to CLAUDE.md in all affected sections (same 273-line layout, same line numbers). Needs the same corrections; kept as its own packet so an isolated agent edits it without touching CLAUDE.md.", "changes": [{"title": "Apply the same Vite corrections as CLAUDE.md", "severity": "high", "instruction": "Apply the identical replacements from the CLAUDE.md packet at the same line numbers: Development Commands (8-12), the build.sh sentence (15), Project Configuration (18-21), Testing Deployment Locally (248-251), Build Artifacts (269-272). build/ -> dist/, npm start -> npm run dev (port 5173), remove build:consistent and npm test, remove build.sh references."}, {"title": "Correct env-var prefixes to VITE_*", "severity": "high", "instruction": "AGENTS.md lines 154-159: rename all REACT_APP_* vars to VITE_* and delete the REACT_APP_DATA_DOMAIN line; fix Common Pitfalls line 243 to 'Must start with VITE_'."}, {"title": "Fix framework identifier", "severity": "high", "instruction": "AGENTS.md line 18: '**Framework**: Create React App with TypeScript' -> '**Framework**: Vite 8 + React 19 + TypeScript'."}], "extraNotes": ""}, {"file": "README.md", "isNew": false, "rationale": "Currently 100% unmodified CRA boilerplate; replace wholesale with accurate Vite project docs.", "changes": [{"title": "Replace CRA boilerplate README with Vite project docs", "severity": "high", "instruction": "Overwrite README.md entirely: title 'XenForo AI Chat Page'; one-line description (TypeScript React 19 chat UI embedded in windowsforum.com/chatpage); Quick Start (npm install; npm run dev -> http://localhost:5173); Available Scripts (dev, build -> dist/, preview, typecheck, deploy); Tech Stack (Vite 8 + React 19 + TypeScript, MUI 9 + Emotion, marked + DOMPurify, XenForo PHP endpoints /chat.php & /tts.php); Environment Setup (create .env with VITE_* vars, see CLAUDE.md); Documentation pointer to ./CLAUDE.md. Remove all Create React App links, npm test/eject references, and the localhost:3000 mention."}], "extraNotes": ""}, {"file": "eslint.config.js", "isNew": true, "rationale": "Project has no ESLint config; a minimal flat config gives contributors a lint command without affecting vite build. Kept minimal and opt-in via a script so it cannot break the build.", "changes": [{"title": "Add a minimal flat ESLint config (build-safe, opt-in)", "severity": "low", "instruction": "Create eslint.config.js as an ES module flat config (package is type:module): @eslint/js recommended + typescript-eslint recommended + eslint-plugin-react-hooks, scoped to src/**/*.{ts,tsx}, ignoring dist/ and node_modules/. Add devDependencies (eslint, @eslint/js, typescript-eslint, eslint-plugin-react-hooks) and an opt-in script `\"lint\": \"eslint src --max-warnings=0 || true\"`. ESLint must NOT be wired into build/deploy so vite build stays green regardless of findings. If the ESLint toolchain risks install/build instability here, downgrade this packet to a documented TODO rather than break the build."}], "extraNotes": "Deps are ALREADY installed (eslint@10, @eslint/js@10, typescript-eslint@8, eslint-plugin-react-hooks@7, globals). The `lint` script (`eslint .`) is ALREADY in package.json — do NOT edit package.json. Create an ESM flat config (package is type:module). Include: a global ignores entry ['dist/**','node_modules/**','build/**','coverage/**','.claude/**']; @eslint/js recommended; typescript-eslint recommended applied to '**/*.{ts,tsx}'; eslint-plugin-react-hooks flat recommended (in v7 use `reactHooks.configs['recommended-latest']`, or register the plugin and enable its recommended rules) applied to '**/*.{ts,tsx}'; languageOptions.globals = globals.browser for tsx. Set rules `@typescript-eslint/no-explicit-any`:'warn' and `react-hooks/exhaustive-deps`:'warn'. GOAL: `npm run lint` must RUN without a config/parse error — it need NOT be warning-free. Verify by running `npm run lint` yourself."}, {"file": "src/__tests__/smoke.test.tsx", "isNew": true, "rationale": "No test runner exists. A single Vitest + jsdom smoke test over pure helpers (the most-changed logic) locks in behavior after this refactor without brittle full-component mounting. Vitest is Vite-native and will not interfere with vite build.", "changes": [{"title": "Add a Vitest smoke test for helpers", "severity": "low", "instruction": "Create src/__tests__/smoke.test.tsx using vitest. Test pure helpers from src/utils/helpers.ts: (1) sanitizeAndParse('Hello **world**') contains '<strong>world</strong>' and no '<script>'; (2) sanitizeAndParse('') === '' (early-return); (3) sanitizeAndParse('[example.com](https://example.com)') contains a Sources block and rel=\"noopener noreferrer\"; (4) generateConversationId() matches /^conv_\\d+_[a-z0-9]+$/; (5) extractTextFromHTML('<p>hi</p>') === 'hi'. Use environment 'jsdom'. Add `\"test\": \"vitest run\"` to package.json scripts and vitest + jsdom to devDependencies, plus a vitest config (a test:{environment:'jsdom'} block in vite.config.ts via /// reference vitest/config, or a separate vitest.config.ts). The setup must NOT change vite build output. If the Vitest toolchain risks environment instability, ship only the test file + script and note the dev install requirement, but never block the build."}], "extraNotes": "You ALSO own a new root file `vitest.config.ts` (no other agent touches it): `import { defineConfig } from 'vitest/config';\\nexport default defineConfig({ test: { environment: 'jsdom', globals: false } });`. The `test`/`test:watch` scripts are ALREADY in package.json — do NOT edit package.json. Write the test as `src/__tests__/smoke.test.ts` (rename from .tsx — no JSX needed) with explicit `import { describe, it, expect } from 'vitest'`. Import helpers from '../utils/helpers'. Assertions (these must pass against the FINAL helpers.ts which gets early-return + rel='noopener noreferrer' + Sources changes in parallel): (1) sanitizeAndParse('Hello **world**') includes '<strong>world</strong>' and not '<script>'; (2) sanitizeAndParse('') === ''; (3) sanitizeAndParse('[example.com](https://example.com)') includes 'Sources' and 'noopener noreferrer'; (4) generateConversationId() matches /^conv_\\d+_[a-z0-9]+$/; (5) extractTextFromHTML('<p>hi</p>') === 'hi'. Run `npm test` yourself and confirm GREEN before finishing."}]
const VERIFY_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["typecheckOk", "buildOk", "testOk", "lintRan", "summary", "failures"], "properties": {"typecheckOk": {"type": "boolean"}, "buildOk": {"type": "boolean"}, "testOk": {"type": "boolean"}, "lintRan": {"type": "boolean", "description": "true if eslint executed without a config/parse crash (warnings are fine)"}, "summary": {"type": "string"}, "failures": {"type": "array", "items": {"type": "object", "additionalProperties": false, "required": ["command", "file", "message"], "properties": {"command": {"type": "string"}, "file": {"type": "string"}, "message": {"type": "string"}}}}}}

function implPrompt(pkt) {
  const changes = pkt.changes.map((c, i) => `  [${i + 1}] (${c.severity}) ${c.title}\n      ${c.instruction}`).join('\n\n')
  return [
    CONTEXT,
    '\nTYPE CONTRACT (pinned, shared across files):\n' + CONTRACT,
    '\nGLOBAL RULES:\n' + RULES,
    `\n=== YOUR FILE: ${pkt.file} ===`,
    pkt.isNew ? 'CREATE this new file.' : 'EDIT this existing file (READ it first).',
    pkt.rationale ? ('rationale: ' + pkt.rationale) : '',
    '\nCHANGES TO APPLY:\n' + changes,
    pkt.extraNotes ? ('\nEXTRA NOTES (override conflicting generic guidance above):\n' + pkt.extraNotes) : '',
  ].filter(Boolean).join('\n')
}

phase('Implement')
log(`Implementing ${PACKETS.length} file packets in parallel — one agent per file, zero edit conflicts`)
const implResults = await parallel(PACKETS.map((pkt) => () =>
  agent(implPrompt(pkt), { label: (pkt.isNew ? 'create:' : 'edit:') + pkt.file, phase: 'Implement' })
    .then((r) => ({ file: pkt.file, ok: true, note: r }))
    .catch((e) => ({ file: pkt.file, ok: false, note: String(e) }))
))
const failedImpl = implResults.filter((r) => !r || !r.ok)
log(`Implementation done: ${implResults.filter((r) => r && r.ok).length}/${PACKETS.length} files written` + (failedImpl.length ? `; ${failedImpl.length} agent error(s)` : ''))

phase('Verify')
const VERIFY_PROMPT = CONTEXT + `

Run the project's gates from the project root and report results precisely. Run each and capture exit codes:
  1) npm run typecheck   2>&1   (tsc --noEmit; exit 0 = pass)
  2) npx vite build      2>&1   (exit 0 = pass; MUST stay green)
  3) npm test            2>&1   (vitest run; exit 0 = pass)
  4) npm run lint        2>&1   (eslint; lintRan=true if it executed without a CONFIG/PARSE crash — warnings/lint errors are NOT a hard failure)

Set typecheckOk/buildOk/testOk by the exit code of (1)(2)(3). For every hard failure (1,2,3) add a failures[] entry with the exact command, the offending file (path:line if shown), and the error message. Do NOT fix anything — just report. Be exact; the repair step depends on your file:line accuracy.`

function repairPrompt(v) {
  return CONTEXT + '\n\nTYPE CONTRACT:\n' + CONTRACT + `

The verify step found HARD failures (typecheck/build/test). Fix them with MINIMAL, surgical edits, preserving the intent of the improvements and all in-flight behavior. You may edit any source file needed. Do NOT edit package.json. Do NOT weaken types back to 'any' to silence errors unless there is genuinely no typed alternative. After fixing, you do NOT need to re-run gates (the next verify pass will). Failures to fix:

${JSON.stringify(v.failures, null, 2)}

Summary from verify: ${v.summary}`
}

let v = null
let attempt = 0
const MAX = 3
while (attempt < MAX) {
  v = await agent(VERIFY_PROMPT, { label: `verify#${attempt + 1}`, phase: 'Verify', schema: VERIFY_SCHEMA })
  const green = v && v.typecheckOk && v.buildOk && v.testOk
  log(`verify#${attempt + 1}: typecheck=${v && v.typecheckOk} build=${v && v.buildOk} test=${v && v.testOk} lintRan=${v && v.lintRan}`)
  if (green) break
  attempt++
  if (attempt >= MAX) break
  log(`repair#${attempt}: fixing ${(v.failures || []).length} failure(s)`)
  await agent(repairPrompt(v), { label: `repair#${attempt}`, phase: 'Verify' })
}

return {
  filesImplemented: implResults.length,
  implAgentErrors: failedImpl.map((r) => r.file),
  verifyAttempts: attempt + 1,
  finalVerify: v,
  green: !!(v && v.typecheckOk && v.buildOk && v.testOk),
}
